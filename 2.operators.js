//! Arithemetic operators
// Addition operator (+) adds two numbers together
const appleOnHand = 10;
const appleRecieved = 42356;

const totalApples = appleOnHand + appleRecieved

// Substraction operator (-) substract one number from another
const totalTickets = 100;
const soldTickets = 4;

const ticketsLeft = totalTickets - soldTickets;

// Multiplication operator (*) multiplies two numbers

// If one book costs 20$ and someone buy 15

const bookCost = 20;
const bookBuy = 15;

const totalCost = bookCost * bookBuy;

// Division operator (/) divids one number by another

//If there are 200 pages in a book and you read 40 pages per day , you will finish in 5 days

const totalPages = 200;
const pagesPerDay = 40;
const daysToFinish = 200 / 40;

// Remainder or modulo perator(%) divides one number by another and returns the reminder 

// if 10 people are at party and there are 3 pizzas , each person get 3 slices, and 1 slice is left over

const peopleParty = 10;
const pizzasAtParty = 3;
const sliceLeftOver = peopleParty % pizzasAtParty;


//Increment operator (++) increases by number 1

// e.g if a website has 100 views and then one person visits, the website now has 1001 views
let currentWebsiteViews = 100;
currentWebsiteViews ++
// or 
// currentWebsiteViews += 1


//decrement operator (--) increases by number 1

let currentWebsiteViewsDecre = 100;
currentWebsiteViewsDecre --
// or 
// currentWebsiteViews -= 1


//! Assignment operators
// +=
// -=
// *=
// /=
// %=
// **=

//! Comparision operators
// Equal to (==)
// Definition: The 'equal to' operator checks if the values of two operands are equal or not, regardless of their type.

// EX: 1 == "1" true

// Strictly Equal to (===)
// Definition: The 'equal to' operator checks if the values of two operands are equal or not, inluding of their type.

// EX: 1 === "1" false

// Not Equal !=
// Definition: The 'Not equal to' operator checks if the values of two operands are different, regardless of their type.

// EX: 1 != "1" false

// Strictly Not Equal to (!==)
// Definition: The 'not strictly qual to' operator checks if the values of two operands are not equal, inluding of their type.

// EX: 1 !== "1" true

// <=
// >=


//! Logical operators
// &&
// ||
// not !

//! String operators
// +

// EX: 1 + "1" 
// O/P 11

// EX: "hasan" + "ali" 
// O/P "hasan ali"

// +=
// EX: let greet = "Hello"
// greet += "Hasan"
